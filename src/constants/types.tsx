export interface Recording {
  id: string;
  title: string;
  artistName: string;
  releaseTitle: string;
  cover: string;
}

export interface Album {
  id: string;
  title: string;
  artistName: string;
  cover: string;
}
